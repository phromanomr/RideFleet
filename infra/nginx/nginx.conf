upstream vrumvrum {
    server api_1:8000;
    server api_2:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://vrumvrum;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}